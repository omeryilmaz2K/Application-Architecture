import 'package:flutter/material.dart';

class NotFoundCardWidget extends StatelessWidget {
  const NotFoundCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Not Found!'),
    );
  }
}
